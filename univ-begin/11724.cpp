#include "bits/stdc++.h"

using namespace std;

int n, m, res;
int u[1010], v[1010];

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> u[i] >> v[i];
    }
    
    return 0;
}